# Dependencias del Servidor de Chat
# Python 3.11+

# Sin dependencias externas - usa solo bibliotecas estándar:
# - socket (networking)
# - threading (concurrencia)
# - json (serialización)
# - hashlib (seguridad)
# - ssl (encriptación)
# - datetime (timestamps)
