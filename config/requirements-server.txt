# Dependencias del Cliente Flet
# Python 3.11+

# Framework de UI
flet==0.24.1

# Pillow para manejo de imágenes
Pillow==10.4.0

# IBM watsonx.ai
ibm-watsonx-ai==1.1.2

# HTTP requests para Giphy API
requests==2.31.0

# Variables de entorno
python-dotenv==1.0.1

# PostgreSQL Database (OPTIONAL - moved to requirements-postgres.txt)
# For PostgreSQL support, install: pip install -r requirements-postgres.txt
# This keeps the app fast by not loading heavy database libraries by default
